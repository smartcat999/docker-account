name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [darwin, linux]
        arch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: Build
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: "0"
        run: |
          go build -trimpath -ldflags "-s -w -X main.version=${GITHUB_REF_NAME}" -o docker-account ./cmd/docker-account
          tar -czf "docker-account_${GOOS}_${GOARCH}.tar.gz" docker-account
      - uses: actions/upload-artifact@v7
        with:
          name: docker-account-${{ matrix.os }}-${{ matrix.arch }}
          path: docker-account_${{ matrix.os }}_${{ matrix.arch }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true
      - name: Prepare versioned installer
        run: |
          grep -q '^release_version="latest"$' install.sh
          sed "s/^release_version=\"latest\"$/release_version=\"${GITHUB_REF_NAME}\"/" install.sh > dist/install.sh
          chmod 755 dist/install.sh
          sh -n dist/install.sh
      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cd dist
          sha256sum *.tar.gz install.sh > checksums.txt
          gh release create "$GITHUB_REF_NAME" *.tar.gz install.sh checksums.txt --repo "$GITHUB_REPOSITORY" --verify-tag --generate-notes
